problem_name: remove_odd_indices

problem_description: |
  Return a new list without elements of <code>lst</code> that have odd indices if <code>odd</code> is True.
  Otherwise, return a new list without even indexed elements.
  <pre><code>
    >>> s = [1, 2, 3, 4]
    >>> t = remove_odd_indices(s, True)
    >>> s
    [1, 2, 3, 4]
    >>> t
    [1, 3]
    >>> l = [5, 6, 7, 8]
    >>> m = remove_odd_indices(l, False)
    >>> m
    [6, 8]
  </code></pre>

code_lines: |
  def remove_odd_indices(lst, odd): #0given
  if !BLANK: #blank #1given
  return !BLANK #blank 
  if !BLANK: #blank
  return !BLANK #blank
  else:
  return !BLANK #blank

test_fn: remove_odd_indices